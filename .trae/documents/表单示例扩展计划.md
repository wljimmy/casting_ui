# 表单示例扩展计划

## 1. 需求分析
需要扩展现有的 `form-test.html`，增加更全面的示例内容，展示不同列情况下各种 Input 组件的展示情况，尽可能呈现所有可能性。

## 2. 现有情况分析
- **当前文件**：`/Users/wanglin/工作_本地/trae/Casting_UI/src/test/form-test.html`
- **已有的组件**：文本输入、下拉选择、日期输入、密码等基础组件
- **已有的列布局**：12列、8列、4列、标签在上、跨列示例
- **已有的 Input 测试**：更多组件示例在 input-test.html 中

## 3. 实现计划

### 3.1 扩展 12 列布局示例
- 增加所有支持自动图标的类型
- 增加 textarea 多行文本域
- 增加 select 下拉选择
- 增加 checkbox 复选框
- 增加 radio 单选框
- 增加 CUI-input-box 组合组件

### 3.2 扩展 8 列布局示例
- 同样增加多种类型组件
- 增加跨列混合布局示例
- 增加不同长度标签的对齐测试
- 增加简约样式（CUI-input--simple）示例

### 3.3 扩展 4 列布局示例
- 展示单列情况下各组件的表现
- 增加带提示和错误状态的完整示例
- 增加 CUI-input-box 组合组件示例

### 3.4 新增 2 列布局示例
- 测试最少列数情况下的布局
- 展示移动端友好的单列布局

### 3.5 新增完整表单示例
- 用户注册表单
- 用户信息编辑表单
- 搜索筛选表单

### 3.6 新增特殊场景示例
- 极长标签 vs 短标签的对齐
- 高组件 vs 矮组件的对齐
- 禁用状态
- 只读状态

## 4. 执行步骤
1. 读取现有 form-test.html
2. 重构页面结构，增加清晰的分区标题
3. 扩展每个列布局的示例内容
4. 新增完整表单示例
5. 新增特殊场景示例
6. 测试和验证布局效果

## 5. 涉及的文件
- `/Users/wanglin/工作_本地/trae/Casting_UI/src/test/form-test.html`
- 可能需要微调 `/Users/wanglin/工作_本地/trae/Casting_UI/src/modules/css/form.css`
