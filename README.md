# 原生HTML5+CSS+JS UI框架

一套可复用的原生HTML5+CSS+JS UI框架，无框架依赖，可直接浏览器运行，适配手机/Pad/PC多端。

## 快速开始

### 克隆命令
```bash
git clone [仓库地址]
```

### 运行方式
1. 双击 `index.html` 文件即可在浏览器中运行
2. 无需服务器部署，纯静态文件

## 目录结构

```
├── index.html    # 单页展示所有UI控件
├── style.css     # 所有样式定义
├── app.js        # 所有交互逻辑
└── README.md     # 框架文档
```

## 技术特点

- **无依赖**：纯原生HTML5+CSS+JS实现，无需任何框架
- **响应式**：基于Flex+Grid+容器查询实现多端适配
- **现代化**：采用科技蓝(#165DFF)为主的流行配色
- **性能优化**：图片懒加载、content-visibility优化
- **流畅交互**：所有控件带原生过渡动画

## 控件清单

### 1. 基础布局组件
- **卡片容器**：9种尺寸组合，3种样式，3种圆角，3种状态
- **通用容器**：流式/固定宽/网格容器，折叠面板，布局容器

### 2. 交互控件
- **按钮**：3种尺寸，5种样式，4种状态
- **输入框**：5种类型，4种状态，带提示
- **选择控件**：单选/复选，下拉选择器，开关，滑块，日期选择器

### 3. 反馈组件
- **弹窗**：5种类型，4种位置，3种样式
- **Toast**：4种状态，3种位置，2种样式
- **加载类**：骨架屏，加载动画，页面级加载遮罩

### 4. 数据展示组件
- **图片控件**：3种样式，带放大/懒加载功能
- **列表**：4种类型
- **表格**：4种类型
- **标签/徽章**：3种尺寸，2种状态，2种样式
- **进度条**：3种类型

### 5. 通用UI元素
- **文本**：标题，正文，说明文本，链接
- **图标**：功能/状态图标，可点击图标
- **分隔组件**：分割线，间距容器，空白占位符

## 自定义说明

### 修改配色
在 `style.css` 文件中修改CSS变量定义：

```css
:root {
    /* 主色系 */
    --primary-color: #165DFF;  /* 修改为主色调 */
    /* 其他颜色变量 */
}
```

### 修改尺寸
在 `style.css` 文件中修改尺寸变量：

```css
:root {
    /* 尺寸 */
    --size-xs: 4px;    /* 最小尺寸 */
    --size-sm: 8px;    /* 小尺寸 */
    --size-md: 16px;   /* 中等尺寸 */
    --size-lg: 24px;   /* 大尺寸 */
    --size-xl: 32px;   /* 超大尺寸 */
}
```

### 修改交互
在 `app.js` 文件中修改交互逻辑：

```javascript
// 示例：修改Toast显示逻辑
this.toast.showToast = (type, message, options) => {
    // 自定义实现
};
```

## 兼容性

- **现代浏览器**：Chrome, Firefox, Safari, Edge 最新版本
- **移动浏览器**：iOS Safari, Android Chrome 最新版本
- **不支持**：IE浏览器

## 使用建议

1. 直接复制 `index.html`、`style.css` 和 `app.js` 文件到项目中
2. 根据需要修改CSS变量和交互逻辑
3. 按需使用所需控件，避免引入不必要的代码

## 贡献

欢迎提交Issue和Pull Request，帮助完善这个UI框架。